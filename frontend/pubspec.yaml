name: bigger_brew
description: App for managing beers.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation:
  dartz:
  flutter_bloc:
  auto_route:
  flutter_hooks:
  get_it:
  injectable:
  firebase_core:
  firebase_auth:
  google_sign_in:
  auth_buttons: ^0.0.4
  json_annotation: ^3.1.0
  flushbar: ^1.10.4
  http: ^0.12.2
  cupertino_icons: ^1.0.0
  giffy_dialog: ^1.8.0
  shared_preferences: ^0.5.12+4
  carousel_slider: ^2.3.1
  image_picker: ^0.6.7+17

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  freezed:
  auto_route_generator:
  injectable_generator:
  json_serializable: ^3.5.0

flutter:
  uses-material-design: true

  assets:
    - assets/fail.gif
